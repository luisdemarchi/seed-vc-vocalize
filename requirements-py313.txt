# Optional environment for Python 3.13 (V2-focused)
# Keep requirements.txt for Python 3.12 stable usage.
# Notes:
# - Prefer NumPy 2.x and SciPy >=1.14 for Python 3.13.
# - PyTorch stable wheels for 3.13 may not be widely available yet; use nightly if needed.
# - Exclude funasr/modelscope to reduce breakage on 3.13; V2 does not require them.
# - Install ffmpeg on your system for pydub if you plan to load/export audio via AudioSegment.

# Core ML/runtime
accelerate>=1.7.0
hydra-core==1.3.2
transformers==4.46.3
huggingface-hub>=0.28.1
einops==0.8.0

# PyTorch (choose one set that matches your platform)
# Linux/CUDA (example; adjust cuda version/index accordingly):
# --extra-index-url https://download.pytorch.org/whl/cu129
# torch --pre
# torchvision --pre
# torchaudio --pre
#
# CPU/macOS (nightly CPU example):
--extra-index-url https://download.pytorch.org/whl/nightly/cpu
torch --pre
torchvision --pre
torchaudio --pre

# Numeric & signal
numpy>=2.1.0
scipy>=1.14.0
librosa==0.10.2.post1
soundfile==0.12.1

# UI / server / utilities
gradio==5.23.0
pydub==0.25.1
python-dotenv>=1.1.0
pyyaml>=6.0.2
munch==4.0.0
matplotlib

# V1-specific (DAC)
descript-audio-codec==1.0.0

# Optional (only if you need them on 3.13 and wheels exist for your platform)
# modelscope==1.18.1
# funasr==1.1.5

# Not required for V2 core but kept if your workflows rely on them
jiwer==3.0.3
resemblyzer>=0.1.4

# Windows-only optional optimization (ignored elsewhere)
# triton-windows==3.2.0.post13
