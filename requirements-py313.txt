#############################################
# Seed-VC - Python 3.13 (Colab/GPU friendly)
#############################################

# IMPORTANT:
# - This file is tailored for Google Colab (Python 3.13) with GPU.
# - It installs PyTorch with CUDA 12.1 wheels and all app dependencies.
# - ffmpeg must be installed via apt on Colab: `!apt-get -y install ffmpeg libsndfile1`.

# PyTorch CUDA 12.1 wheels
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Core ML/runtime
accelerate>=1.7.0
hydra-core==1.3.2
transformers==4.46.3
huggingface-hub>=0.28.1
einops==0.8.0

# Numeric & signal
numpy>=2.1.0
scipy>=1.14.0
librosa==0.10.2.post1
soundfile==0.12.1
soxr

# UI / server / utilities
gradio==5.23.0
pydub==0.25.1
python-dotenv>=1.1.0
pyyaml>=6.0.2
munch==4.0.0
matplotlib

# V1-specific (DAC)
descript-audio-codec==1.0.0

# Not required for V2 core but commonly useful
jiwer==3.0.3
resemblyzer>=0.1.4

# (Optional) Windows-only optimization (ignored elsewhere)
# triton-windows==3.2.0.post13
